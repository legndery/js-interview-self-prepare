const arr = [
`Sort an array in O(N) which is of size N and contains elements from 0 to N - 1 without duplicates.`

];

module.exports = arr;


// 1. Usual answers
// 2. Get index using % N to place the current item while iterating linearly.
// 3. Keep track of the lastRead and broadcast of the length of the file increases to all connecting outputstreams from response.
// 4. Use the History sqllite file from chrome in User Data/Profile 1. Screenshot using Selenium's Webdriver.